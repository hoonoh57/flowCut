import{useEffect}from'react';
import{useEditorStore}from'../stores/editorStore';
import{DeleteClipCommand}from'../stores/commands/DeleteClipCommand';
import{SplitClipCommand}from'../stores/commands/SplitClipCommand';
export function useKeyboardShortcuts(){const store=useEditorStore;useEffect(()=>{const h=(e:KeyboardEvent)=>{const s=store.getState();const tag=(e.target as HTMLElement)?.tagName;if(tag==='INPUT'||tag==='TEXTAREA'||tag==='SELECT')return;const ctrl=e.ctrlKey||e.metaKey;if(ctrl&&e.key==='z'&&!e.shiftKey){e.preventDefault();s.undo();return;}if((ctrl&&e.key==='y')||(ctrl&&e.key==='z'&&e.shiftKey)){e.preventDefault();s.redo();return;}if(e.key==='Delete'||e.key==='Backspace'){e.preventDefault();s.selectedClipIds.forEach(id=>s.dispatch(new DeleteClipCommand(id)));return;}if(ctrl&&e.key==='b'){e.preventDefault();s.selectedClipIds.forEach(id=>s.dispatch(new SplitClipCommand(id,s.currentFrame)));return;}if(e.key===' '){e.preventDefault();s.togglePlayback();return;}if(e.key==='ArrowLeft'){e.preventDefault();s.setCurrentFrame(s.currentFrame-(e.shiftKey?10:1));return;}if(e.key==='ArrowRight'){e.preventDefault();s.setCurrentFrame(s.currentFrame+(e.shiftKey?10:1));return;}if(e.key==='s'&&!ctrl){e.preventDefault();s.setSnapEnabled(!s.snapEnabled);return;}};window.addEventListener('keydown',h);return()=>window.removeEventListener('keydown',h);},[]);}
